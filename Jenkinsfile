@Library('library-demo') _

pipeline {
    agent any
    stages{
         stage("Replacement String"){
            steps{
                script{
                   sh '''
                        ls
                        git branch -a
                   ''' 
        }
    }
}

    }}